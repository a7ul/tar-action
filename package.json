{
  "name": "tar-action",
  "version": "1.0.0",
  "description": "A github action to use tar in all platforms",
  "main": "index.js",
  "scripts": {
    "build": "parcel build src/index.js --target node --no-source-maps --bundle-node-modules -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "atulanand94@gmail.com",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.2",
    "@actions/github": "^2.1.1",
    "tar": "^6.0.1"
  },
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
